// insta_home.dart

import 'package:flutter/material.dart';

class InstagramHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Instagram Education App'),
      ),
      body: Center(child: Text('Instagram Education App')),
    );
  }
}
// custom_divider.dart

import 'package:flutter/material.dart';

class CustomDivider extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Divider(
      height: 1,
      thickness: 2.0, // Set your preferred thickness
      color: Colors.black, // Set your preferred color
    );
  }
}
